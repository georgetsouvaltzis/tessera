namespace Tessera.Styles;

/// <summary>
/// Represents common visual states used by theme override resolution.
/// </summary>
public enum TesseraThemeVisualState
{
    Default = 0,
    Focused = 1,
    Hovered = 2,
    Selected = 3,
    Disabled = 4,
    Error = 5,
    Active = 6,
}
