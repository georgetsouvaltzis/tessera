# Perf Baseline V1 Smoke

Date (UTC): 2026-03-19 16:22:51Z

Environment:
- host: `Darwin Georges-MacBook-Pro-2.local 25.1.0 ... arm64`
- terminal: `xterm-ghostty`
- SDK/runtime from benchmark output: `.NET SDK 10.0.103`, host runtime `.NET 10.0.3`

Goal: prove expanded harness is runnable with a lightweight smoke path.

Commands and outcomes:
1. `dotnet run --project benchmarks/TeaSharp.Benchmarks -c Release --no-build -- --filter "*Startup*|*LargeTable*"`
   - returned 0 benchmarks (BenchmarkDotNet filter expression form mismatch).
2. `dotnet run --project benchmarks/TeaSharp.Benchmarks -c Release --no-build -- --filter "*Startup*" "*LargeTable*"`
   - build blocked in autogenerated project by repo guardrail:
   - `error : TeaSharp forbids unsafe code. Remove /unsafe or AllowUnsafeBlocks=true.`
3. Smoke execution proof (in-process toolchain):
   - `dotnet run --project benchmarks/TeaSharp.Benchmarks -c Release --no-build -- --inProcess --filter "*Startup*"`
   - `dotnet run --project benchmarks/TeaSharp.Benchmarks -c Release --no-build -- --inProcess --filter "*LargeTable*"`

Key output excerpts:
- `Found 1 benchmarks: StartupRenderBenchmarks...`
- `Mean = 14.708 us ... Allocated = 50.17 KB`
- `Found 1 benchmarks: LargeTableBenchmarks...`
- `Mean = 22.708 us ... Allocated = 78.38 KB`

Notes:
- both smoke runs completed with `executed benchmarks: 1`
- process priority warning observed in this environment (`Permission denied`), benchmark execution still completed
